/**
 * Redis client placeholder.
 * TODO: Efekan — Install ioredis and configure.
 * Used for: session store, publish queue, rate limiting.
 */

// import Redis from "ioredis";
// export const redis = new Redis(process.env.REDIS_URL);

export const redis = null; // placeholder
